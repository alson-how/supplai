import {describe,it,expect} from 'vitest'; import request from 'supertest'; import {createApp} from './app.js';
describe('API security and workflow',()=>{it('rejects unauthenticated analytics',async()=>expect((await request(createApp()).get('/api/analytics/executive-summary')).status).toBe(401));it('logs in and records an approval',async()=>{const app=createApp();const login=await request(app).post('/api/auth/login').send({email:'commercial_planner@demo.supplai.io',password:'Demo@123'});expect(login.status).toBe(200);const token=login.body.accessToken;const decision=await request(app).patch('/api/recommendations/rec-1/decision').set('Authorization',`Bearer ${token}`).send({decision:'APPROVED',reason:'Commercial and supply review complete'});expect(decision.body.status).toBe('APPROVED');expect((await request(app).get('/api/audit').set('Authorization',`Bearer ${token}`)).body.total).toBeGreaterThan(0);});});
